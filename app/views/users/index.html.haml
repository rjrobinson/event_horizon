- content_for(:title) do
  Users - Launch Academy

%ul.breadcrumbs
  %li.current= link_to "Users", users_path

%h2 Users

%ul.block
  - @users.each do |user|
    %li= link_to user.username, user_path(user)
